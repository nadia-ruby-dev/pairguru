- @movie.comments.each do |c|
  - if c.persisted?
    %blockquote.comment
    %p= c.text
    %cite
      = c.user.email
    - if current_user == c.user
      = link_to "Delete comment", movie_comment_path(@movie),
      method: :delete, data: { confirm: "Are you sure you want to delete this comment?" }, class: "btn btn-danger"
